steps:
    -   type: class
        identifier: folder
        current_version_eq: 0.0.0
    -   type: steps
        condition: $test_is_missing
        identifier: sub-3
        steps:
            -   type: content